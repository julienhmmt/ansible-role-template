---
- name: "Verify (no_service)"
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: "Gather package facts"
      ansible.builtin.package_facts:
        manager: auto

    # Exemple : vérifier installation sans service
    # - name: "Assert package is installed"
    #   ansible.builtin.assert:
    #     that:
    #       - "'example-package' in ansible_facts.packages"

    - name: "Verification placeholder (no_service)"
      ansible.builtin.debug:
        msg: "Verification passed (no_service scenario)"
